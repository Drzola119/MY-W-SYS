# Component — StaffBios

Team/staff member cards: photo, name, role, one-line bio. Real people only
— sourced from `research/` (About page, social bios, Google profile
photos with captions). If no staff info exists, this component is omitted
from the sitemap entirely rather than populated with placeholders.

Props: `staff: [{ name, role, bio, photo? }]`.
