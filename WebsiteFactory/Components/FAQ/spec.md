# Component — FAQ

Accordion. Questions should be the ones real visitors actually have for
this sub-vertical (see each `Templates/*.md`'s FAQ suggestions) — not a
generic filler list. Keyboard-accessible accordion (not just click),
proper `aria-expanded` state.

Props: `items: [{ question, answer }]`.
