# Component — ContactLocation

Map embed + hours + phone + address. This data must match the Google
Business Profile exactly (`Standards/quality-standards.md` — NAP
consistency matters for local SEO, not just correctness).

Props: `address`, `phone`, `hours: [{ day, open, close }]`, `mapEmbedUrl`.
